name: Build and deploy Node.js app to Azure Web App - coordina-backend2

on:
  push:
    branches:
      - main
  workflow_dispatc

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'

      - name: Install backend dependencies
        working-directory: ./backend
        run: npm install

      - name: Build backend (TypeScript → JavaScript)
        working-directory: ./backend
        run: npm run build

      - name: Upload backend dist folder
        uses: actions/upload-artifact@v4
        with:
          name: backend2-dist
          path: backend/dist/

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download backend dist artifact
        uses: actions/download-artifact@v4
        with:
          name: backend2-dist
          path: .

      - name: Azure Login (using original working client-id)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_8C23322913F04826A0702D9DE971572B }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_885EE270E1FF4FB799DEA764650F1049 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_2E187219C5F847CBBC25D690B976424C }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'coordina-backend2'
          slot-name: Production
          package: backend2-dist
